# Persistenz
# Dev-Default ist SQLite (funktioniert sofort):
DATABASE_URL=sqlite+pysqlite:///./data/stellwerk.db
# Preprod/Prod: Postgres (z. B. via docker-compose.preprod.yml)

# Debug-Konsole in der Webapp (SSE)
STELLWERK_DEBUG=1

# OpenAI-kompatibler Endpoint (für Planung erforderlich)
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Robustheit (Netzwerk/Rate-Limits)
# Total-Timeout (Sekunden) für OpenAI-Requests
OPENAI_TIMEOUT_SECONDS=60
# Retries bei ReadTimeout/429/5xx
OPENAI_RETRIES=2
